# Replaceable Assets

Put future mascot art, meal placeholders, icons, and background textures here.

The MVP currently uses CSS and simple SVG-style shapes in components so the app can run without external image dependencies.
